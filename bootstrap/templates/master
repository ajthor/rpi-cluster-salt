{% set remotes = salt['pillar.get']('config:gitfs_remotes', ['https://github.com/ajthor/rpi-cluster-salt.git']) %}

file_roots:
  base:
    - /srv/salt

fileserver_backend:
  - git
  - roots

gitfs_remotes:
{% for r in remotes %}
  - {{ r }}
{%- endfor %}

pillar_roots:
  base:
    - /srv/pillar
