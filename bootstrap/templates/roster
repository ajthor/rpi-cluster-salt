# Use the following template when adding new nodes to the roster:
#
# rpi-node-1:
#   host: rpi-node-1.local
#   user: pi
#   passwd: raspberry
#   sudo: True
#

{% for minion in salt['pillar.get']('config:cluster:minions') %}
{{ minion }}:
    host: {{ minion }}.local
    user: pi
    passwd: raspberry
    sudo: True
{% endfor %}
