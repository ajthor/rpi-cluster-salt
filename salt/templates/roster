# Use the following template when adding new nodes to the roster:
#
# rpi-node-1:
#   host: rpi-node-1.local
#   user: pi
#   passwd: raspberry
#   sudo: True
#

{% set minions = salt['pillar.get']('config:minions') %}

{% for m in minions %}
{{ m }}:
    host: {{ m }}.local
    user: pi
    passwd: raspberry
    sudo: True
{% endfor %}
